---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<article class="card" data-slot="Card">
  <h3>{title}</h3>
  <p>{description}</p>
</article>
